var outputElement = document.getElementById("output");

// Function to display the text on the screen
function displayText() {
  outputElement.textContent = "adressen database";
}

// Call the displayText function when the DOM content is loaded
document.addEventListener("DOMContentLoaded", displayText);